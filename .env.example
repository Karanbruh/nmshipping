# RSS2JSON API key for Indian cricket news feed (free tier: https://rss2json.com)
VITE_RSS2JSON_API_KEY=your_key_here

# Optional: backend proxy URL for production (when backend is available)
# In local dev, /api/cricket-news proxy is used automatically when this is unset.
# VITE_NEWS_API_URL=/api/cricket-news
